#include "ChipColor.h"
// --- META ---

ENUM_META(ChipColor)
{
    ENUM_ENTRY(Blue);
    ENUM_ENTRY(Green);
    ENUM_ENTRY(Red);
    ENUM_ENTRY(Violet);
    ENUM_ENTRY(Yellow);
}
END_ENUM_META;
// --- END META ---
